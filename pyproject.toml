[project]
name = "monitoring-publication-bal-mss"
version = "0.1.0"
description = "Supervision de la publication des BAL MSSanté dans l'Annuaire Santé"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
